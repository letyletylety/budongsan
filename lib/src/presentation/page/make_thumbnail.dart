import 'package:flutter/material.dart';

class MakeThumbNail extends StatelessWidget {
  const MakeThumbNail({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('썸네일 만들기'),
      ),
      body: Container(),
    );
  }
}
